{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd artifacts/api-server && pnpm exec tsc -p tsconfig.json --noEmit && pnpm exec esbuild src/app.ts --bundle --platform=node --format=cjs --outfile=dist/app.cjs --external:pino --external:pg-native",
  "functions": {
    "api/index.ts": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)",     "destination": "/api/index" }
  ]
}
